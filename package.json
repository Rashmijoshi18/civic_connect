{
  "name": "civicconnect",
  "version": "1.0.0",
  "private": true,
  "description": "CivicConnect - Community Problem Reporting & Solution Platform",
  "scripts": {
    "install:all": "npm --prefix client install && npm --prefix server install",
    "build:client": "npm --prefix client run build",
    "prisma:generate": "npm --prefix server run vercel-build",
    "build": "npm --prefix client run build && npm --prefix server run vercel-build",
    "vercel-build": "npm --prefix client install && npm --prefix client run build && npm --prefix server install && npm --prefix server run vercel-build",
    "dev": "npm --prefix client run dev",
    "server:dev": "npm --prefix server run dev",
    "server:start": "npm --prefix server start",
    "seed": "npm --prefix server run seed"
  }
}
